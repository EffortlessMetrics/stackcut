# 01 feature plus refactor

A rename-only prep change lands in the same range as planner behavior edits plus validating tests and docs.

Expected result:

- peel the rename into a mechanical slice
- keep behavior, tests, and docs together
- order the behavior slice after the prep slice
